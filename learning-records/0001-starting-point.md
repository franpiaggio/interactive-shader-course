# Starting point: new to shaders, mission is raymarched art

Established at intake (2026-06-23). The user is **new to shaders** (no assumed GLSL),
but the mission is **creative raymarched art** (Shadertoy-style), not web/engine production.

This sets the floor and the trajectory for future sessions:
- **Floor:** teach fragment-shader fundamentals from zero — do not assume GLSL syntax,
  types, or the per-pixel model. Lesson 0001 covers the per-pixel model + `uv`.
- **Trajectory:** keep raymarching as the visible north star from lesson 1 so fundamentals
  feel purposeful, not abstract. Ladder lives in [[NOTES.md]]; mission in [[MISSION.md]].
- **Environment:** the user wants code that runs locally and on Shadertoy unchanged — the
  `assets/shader-lab.js` harness enforces this, so all lessons stay Shadertoy-portable.

No demonstrated skill yet — update once the user completes Lesson 01's challenges (evidence of
the per-pixel / `uv` model sticking).
